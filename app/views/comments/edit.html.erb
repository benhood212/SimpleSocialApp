<h1>Edit Comment</h1>

<%= form_with model: @comment, url: post_comment_path(@post, @comment), method: :patch do |form| %>
  <%= form.label :content %>
  <%= form.text_area :content, value: @comment.content %>
  <%= form.submit "Update Comment" %>
<% end %>

<%= link_to "Back to Post", @post %>
